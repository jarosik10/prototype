import styled from 'styled-components';

const H2 = styled.h2`
    text-align: center;
    color: ${({ theme }) => theme.colors.blue};
    font-size: 45px;
    margin: 70px 0 40px;
`;

export default H2;